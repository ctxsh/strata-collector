apiVersion: strata.ctx.sh/v1beta1
kind: Discovery
metadata:
  name: example
  namespace: default
spec:
  enabled: false
  prefix: prometheus.io
  intervalSeconds: 10
  # selector:
  #   matchLabels:
  #     app: example
  collector:
    matchLabels:
      sink: example
